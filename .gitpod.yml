tasks:
  - init: |
      pip install jupyterlab rise
      jupyter-nbextension install rise --py --sys-prefix
      jupyter-nbextension enable rise --py --sys-prefix
    # No command to start Jupyter Lab automatically

vscode:
  extensions:
    - ms-python.python
